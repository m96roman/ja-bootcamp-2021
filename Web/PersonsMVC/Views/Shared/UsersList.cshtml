@model IEnumerable<UserViewModel>;

<div class="row">
    @if (Model.Count() == 0)
    {<h3 class="text-danger">Sequence is empty</h3> }
    else
    {
        @foreach (var item in Model)
        {
            <div class="col-sm-4 mb-3">

                @await Html.PartialAsync("UserView", item)

                <div>
                    <a class="btn btn-warning btn-edit" id="btn-edit-@item.Id">Edit</a>
                    @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-primary" })
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger btn-delete", id = $"btn-delete-{item.Id}" })
                </div>
            </div>
        }
    }
</div>